# African Guard Architecture

African Guard is structured as a cloud-native FinCrime operating system. The local implementation runs with simulated adapters by default, but the service boundaries, data contracts, and deployment surfaces are production-oriented.

## Services

| Service | Runtime | Responsibility |
| --- | --- | --- |
| `apps/web` | Next.js | Analyst, investigator, compliance, admin, and partner operating console. |
| `services/api` | NestJS | Fraud scoring API, case management, payment recall, collaboration intelligence, WebSocket stream, RBAC, audit, and rate limiting. |
| `services/ai` | FastAPI | ML anomaly scoring, explainability payloads, copilot summaries, SAR drafts, drift telemetry, and feedback ingestion. |
| `postgres` | PostgreSQL + Prisma | Core event, alert, payment, case, institution, audit, and feedback records. |
| `redis` | Redis Streams/cache | Low-latency alert fanout, rate state, and hot decision cache. |
| `kafka` | Kafka | Durable event bus for financial events and fraud intelligence propagation. |
| `neo4j` | Graph database | Identity graph, mule network detection, fraud-ring linkage, and graph risk propagation. |

## Pillar Mapping

| Pillar | Implemented Surface |
| --- | --- |
| Real-time fraud detection | `POST /v1/events/score`, `fraud.event.scored` WebSocket event, hybrid score JSON contract. |
| Fraud intelligence network | `GET /v1/intelligence/network`, encrypted consortium package endpoint, institution trust scores, typology library. |
| Payment tracking and recall | `GET /v1/payments`, `POST /v1/payments/:id/recall`, route visualization and recall state mutation. |
| AI investigation copilot | `GET /v1/cases/:id/copilot`, `POST /v1/cases/:id/chat`, FastAPI copilot endpoints. |
| Identity graph intelligence | `GET /v1/graph`, graph view, risk propagation in scoring components. |
| Case management | `GET /v1/cases`, `POST /v1/cases/:id/status`, critical-risk auto-case creation. |
| Real-time dashboard | Socket.IO gateway emits `operating.picture`, `fraud.alert`, and `fraud.event.scored`. |
| Digital identity and behavior | Device, IP, geo-velocity, email, phone, velocity, and account-age signals in the scoring contract. |
| AML plus fraud convergence | `GET /v1/aml/customers`, AML/sanctions scoring component, unified customer risk view. |
| AI learning | `POST /v1/learning/feedback`, model drift telemetry, feedback queue and false-positive optimization metrics. |
| Agentic fraud and AML ops | `GET /v1/agents/ops`, `POST /v1/agents/run`, natural-language agents for data analysis, graph analysis, OSINT, rule optimization, dispute evidence, and AML queue triage. |
| Merchant risk monitoring | Merchant collusion scores, shared-user concentration, dormant merchant session spikes, no-acquiring-transaction controls, and merchant account repurposing signals. |
| Dispute management | Visa Compelling Evidence 3.0 qualification checks, three-transaction evidence packages, and dispute filing agent outputs. |
| Guided enablement | `GET /v1/training`, in-app demo walkthrough, subscriber learning paths, certifications, and facilitated training offers. |
| Workforce optimization | Manual-hours-saved, auto-resolution, false-positive queue clearance, redeployable FTE, cost avoidance, and recommendations for moving staff into higher-value work. |

## Real-Time Flow

1. An event is posted to `POST /v1/events/score` or generated by the WebSocket simulator.
2. `fraud-engine-service` computes rule, anomaly, behavior, graph, consortium, and AML scores.
3. Critical and high-risk decisions become live alerts.
4. Critical decisions create cases automatically.
5. The payment, graph, AML, learning, and alert panels receive fresh `operating.picture` events over WebSockets.
6. Analyst labels are accepted by the learning loop and queued for retraining.

## Security Model

- RBAC is enforced through `x-role` in local development and can be replaced by JWT claims.
- Rate limiting is active globally through NestJS throttling.
- Consortium sharing uses HMAC pseudonymisation and AES-256-GCM encrypted envelopes.
- Audit events are created for case status changes, payment recalls, auto-case creation, and learning labels.

## Agentic Operations Layer

The agentic layer is designed to reduce repetitive analyst work while preserving governance:

- Data Analyst Agent: answers merchant, transaction, session, and user-pattern questions.
- Graph Analyst Agent: maps shared devices, users, emails, IPs, merchants, and payment instruments.
- OSINT Search Agent: summarizes registry, domain, adverse media, and ownership findings for EDD.
- Rule Assistant Agent: converts plain-language hypotheses into review, hold, block, or step-up logic with backtest summaries.
- Dispute Filing Agent: assembles chargeback evidence and Visa CE 3.0 packages.
- Agentic AML Ops: clears low-risk queues, escalates high-risk activity, and keeps audit trails.

The current implementation uses deterministic demo data and in-memory agent runs. The intended production adapter boundary is a warehouse or lakehouse query layer plus governed external OSINT providers, card-network dispute APIs, and case-management persistence.
- Secrets are represented as vault references in configuration and `.env.example`.
